# 발표 (2분 30초)

오전에는 고속도로 돌발상황·유가 대시보드를 만들었고, 오후에는 그걸 추석 귀성길 안내판으로 넓혔습니다.

## 1. 해결하고 싶었던 문제
귀성길에 노선을 고를 때 정체·돌발상황, 유가, 지금 도로 상태를 각각 따로 찾아봐야 했습니다. 출발지·도착지만 넣으면(또는 노선 하나만 고르면) 필요한 정보가 한 번에 나오면 좋겠다고 생각했습니다.

## 2. 어떤 데이터로 풀었나
한국도로공사 고속도로 공공데이터 포털의 실시간 돌발상황, 휴게소 유가, 실시간 속도, 교통예보(서울 출발 예상 소요시간), 휴게소 위치 좌표 API를 썼고 전부 `key=test`로 됐습니다. 브라우저가 이 API들을 직접 부르지 않도록 Vercel 서버리스 함수(`api/burst.js` 등)를 사이에 두고, 화면을 열 때마다 그 함수가 도로공사에 새로 물어보는 방식으로 만들어서 진짜 실시간이 되게 했습니다. CCTV는 로드플러스(roadplus.co.kr) 링크로 연결했습니다.

## 3. 실제 구현 과정
PRD → 서버리스 API로 실시간 데이터 받기 → `index.html` 화면 구성 → 배포까지 하고 나서, 확인해보며 계속 다듬었습니다.
- 처음엔 출발지·도착지를 제가 손으로 넣은 도시 24개짜리 표로 찾게 했는데, 실제 데이터(휴게소 주소)에서 시·군 이름을 직접 뽑아내는 방식으로 바꿔서 91개 지역이 되게 했습니다. 고속도로가 안 닿는 지역(울진 등)은 "국도 구간 있음"으로 표시만 따로 했습니다.
- 지도와 고속도로망 그래프를 따로 만들었다가, 노선을 고르면 지도 위에서 그 노선만 굵게 표시되도록 하나로 합쳤습니다.
- 막혔던 것: 실시간 속도 API가 전국 8천 건 넘게 한 번에 와서 화면이 무거워질 뻔했는데, 필요한 항목만 걸러서 줄였습니다. 휴게소 유가와 휴게소 좌표, 두 API를 연결할 공통 키가 이름이 아니라 관리번호였다는 것도 나중에 알아채서 그걸로 다시 연결했습니다.
주소: https://chuseok-xi.vercel.app

## 4. 느낀 것과 앞으로 해볼 것
어려웠고, 이게 맞는 방향인지 아직 확신은 안 섭니다.
다음에는 CCTV 실제 재생 기능과 전국 날씨지도를 더해보고 싶습니다.
